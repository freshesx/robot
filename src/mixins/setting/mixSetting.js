import mix from '../../helpers/mix.js'
// import {
//   Container
// } from 'element-ui'

const Setting = {
  name: 'RtSetting',
  render (h) {
    return (
      <div>Hello world!</div>
    )
  }
}

export default config => mix(Setting, config)
